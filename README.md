# Custom-CSS-Framework

## Project To-Do List

### 1) Build the framework with Sass
- [x] Create the CSS Framework using Sass
- [x] Organize it using Sass partials (multiple `.scss` files that get imported into a main file)

### 2) Add customization support
- [x] Use CSS variables or Sass variables so users can customize the framework (ex: colors, spacing, fonts, etc.)

### 3) Create a theme for standard HTML elements
Make sure the framework styles standard elements (at minimum):
- [x] Headings (`h1` to `h6`)
- [ ] Lists (`ul`, `ol`, `li`)
- [x] Buttons (`button`, link buttons, etc.)
- [ ] Forms (`form`)
- [ ] Inputs (`input`, `select`, `textarea`, etc.)
- [ ] Tables (`table`, `thead`, `tbody`, `tr`, `th`, `td`)
- [ ] Other common base elements (like `p`, `a`, etc.)

### 4) Create utility classes
Provide utility classes for common styling, including:
- [x] Color (text + background)
- [x] Font weight
- [x] Font size
- [x] Margin
- [x] Padding
- [x] Border

### 5) Ensure the theme looks consistent and good
- [ ] Apply the theme consistently across all elements
- [ ] Make sure everything looks appealing together (not just each element on its own)

### 6) Include the compiled CSS in the repo
- [x] Compile your Sass into a final CSS file (ex: `dist/framework.css`)
- [x] Include that compiled output in the repository (not just the `.scss` files)

### 7) Write the README documentation
Add clear instructions for:
- [ ] Installation (how to get it running)
- [ ] Usage (how to use classes and components)
- [ ] Customization (how to change variables / theme settings)